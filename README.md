# CodeSnippet Hub

CodeSnippet Hub - это настольное приложение для хранения и управления кодовыми сниппетами с подсветкой синтаксиса. Это ваша личная библиотека кода, где вы можете хранить полезные куски кода, которые часто используете, но забываете.

## Особенности

- 📝 Создание заметок с заголовком и тегами
- 🎨 Подсветка синтаксиса для различных языков программирования
- 📋 Кнопка "Копировать в буфер" в один клик
- 🔍 Поиск по заголовкам и тегам
- 🧩 Поддержка многоячеистых сниппетов (код, текст, изображения)
- 💾 Локальное хранение данных в SQLite

## Технологии

- **Python** - основной язык программирования
- **Flet** - создание настольного интерфейса
- **SQLite** - локальная база данных
- **PyYAML** - сериализация многоячеистых сниппетов
- **pyperclip** - копирование в буфер обмена
- **uv** - менеджер зависимостей (альтернатива pip)

## Установка и запуск

### Требования

- Python 3.8.1 или выше
- [uv](https://github.com/astral-sh/uv) (рекомендуется) или pip

### Установка с помощью uv (рекомендуется)

1. Установите uv:
   ```bash
   pip install uv
   ```

2. Клонируйте или скачайте этот репозиторий

3. Перейдите в директорию проекта:
   ```bash
   cd codesnippet-hub
   ```

4. Установите зависимости:
   ```bash
   uv sync
   ```

5. Запустите приложение:
   ```bash
   uv run python src/main.py
   ```

### Альтернативная установка с помощью pip

1. Клонируйте или скачайте этот репозиторий

2. Перейдите в директорию проекта:
   ```bash
   cd codesnippet-hub
   ```

3. Создайте виртуальное окружение:
   ```bash
   python -m venv venv
   source venv/bin/activate  # На Linux/Mac
   # или
   venv\Scripts\activate  # На Windows
   ```

4. Установите зависимости:
   ```bash
   pip install flet
   ```

5. Запустите приложение:
   ```bash
   python src/main.py
   ```

## Сборка в исполняемый файл (EXE)

Для создания исполняемого файла используйте Flet pack:

```bash
pip install pyinstaller
flet pack src/main.py --name CodeSnippetHub --icon icon.ico
```

Если у вас нет иконки, можно создать исполняемый файл без нее:

```bash
flet pack src/main.py --name CodeSnippetHub
```

## Структура проекта

```
codesnippet-hub/
├── pyproject.toml          # Конфигурация проекта и зависимости
├── src/
│   ├── main.py            # Главный файл приложения
│   ├── models/
│   │   └── database.py    # Классы для работы с базой данных
│   └── ui/
│       ├── main_view.py   # Главное окно приложения
│       └── components.py  # Компоненты интерфейса
└── README.md              # Документация
```

## Функциональность

### Основной экран

- Поиск сниппетов по названию или тегам
- Кнопка добавления нового сниппета
- Сетка карточек со сниппетами

### Карточка сниппета

- Название и основной язык программирования
- Область с подсветкой синтаксиса для многоячеистых сниппетов
- Кнопки: копировать, редактировать, удалить

### Редактор сниппетов

- Поддержка многоячеистых сниппетов (код, текст, изображения)
- Выбор типа ячейки (код, текст, изображение)
- Выбор языка программирования для ячеек кода
- Возможность добавления нескольких ячеек в одном сниппете

## Стек технологий

- **Frontend**: Flet (Python GUI framework)
- **Backend**: SQLite (встроенная база данных)
- **Архитектура**: MVC-подобная структура с разделением на компоненты

## Демонстрационные данные

При первом запуске приложение автоматически создает демонстрационные сниппеты по следующим темам:

- **Python Data Science**: pandas, numpy, scikit-learn, pytorch
- **API**: requests, fastapi
- **Terminal**: полезные команды
- **Docker**: основные команды и docker-compose
- **Frontend**: HTML + JavaScript
- **Java**: классы и методы
- **C++**: структуры данных
- **SQL**: CRUD операции

## Для курсовой работы

Этот проект демонстрирует:
- Работу с базой данных (SQLite)
- Создание современного UI с Flet
- Принципы объектно-ориентированного программирования
- Разделение кода на модули
- Работу с событиями и асинхронными операциями
- Подсветку синтаксиса кода

## Лицензия

MIT
